CONSTANTS
    Surfaces = {
        llm_prompt,
        tool_call_placeholder,
        tool_backend_method,
        memory_read,
        memory_write,
        delegation,
        request_envelope_replay
    }
    PromptLayer = prompt_layer
    ToolLayer = tool_layer
    MemoryLayer = memory_layer
    DelegationLayer = delegation_layer
    ReplayLayer = replay_layer
    PromptLayerSurfaces = {llm_prompt}
    ToolLayerSurfaces = {tool_call_placeholder, tool_backend_method}
    MemoryLayerSurfaces = {memory_read, memory_write}
    DelegationLayerSurfaces = {delegation}
    ReplayLayerSurfaces = {request_envelope_replay}

SPECIFICATION Spec

INVARIANTS
    LayerExecuteClaim
    LayerCoverageClaim
